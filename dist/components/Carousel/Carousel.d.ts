import React from "react";
import { CarouselProps } from "./types";
export declare const Carousel: React.MemoExoticComponent<({ children, controls, indicators, activeIndex, autoPlay, interval, ...props }: CarouselProps) => JSX.Element>;
